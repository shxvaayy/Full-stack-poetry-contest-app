const fs = require('fs');
const path = require('path');

const filePath = path.join(__dirname, 'server', 'poem-submission-service.json');
const raw = fs.readFileSync(filePath, 'utf8');
const escaped = JSON.stringify(JSON.parse(raw));
console.log(escaped);













